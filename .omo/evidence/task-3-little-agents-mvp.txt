T3 baseline / QA evidence

Commands run:
1. `Test-Path ".gitignore"`
   Result: `False`

2. `Test-Path "LittleAgentsExtension/Properties/launchSettings.json"`
   Result: `True`

3. `Select-String -Path .gitignore -Pattern "launchSettings\.json|\.pubxml"`
   Result: skipped because root `.gitignore` does not exist

Cleanup receipt:
- No temp files created.
