Task 21 evidence - AgentsListPage cache refresh subscriptions

Changed files in scope:
- LittleAgentsExtension/Pages/Agents/AgentsListPage.cs
- .omo/evidence/task-21-little-agents-mvp.txt

Implementation evidence:
- AgentsListPage constructor initializes _cachedAgents with agents.Load().
- AgentsListPage constructor initializes _cachedProviders with providers.Load().
- agents.Changed refreshes _cachedAgents = agents.Load(), then calls RaiseItemsChanged(-1).
- providers.Changed refreshes _cachedProviders = providers.Load(), then calls RaiseItemsChanged(-1).
- GetItems uses _cachedAgents and does not call agents.Load().

Command:
dotnet build "LittleAgentsExtension/LittleAgentsExtension.csproj" -c Debug -p:Platform=x64 2>&1; "EXITCODE=$LASTEXITCODE"
Result:
- Restore: all projects up-to-date.
- Build output: LittleAgentsExtension.dll written under bin/x64/Debug/net9.0-windows10.0.26100.0/win-x64.
- Warnings: 3 existing warnings reported (CS8424 in ILlmChatClient.cs, CsWinRT1028 in generated GlobalFreeSafeHandle.g.cs, CA1822 in ClipboardReader.cs).
- Errors: 0.
- EXITCODE=0.

Command:
(Get-Content "LittleAgentsExtension/Pages/Agents/AgentsListPage.cs").Count
Result:
125

Command:
$path = "LittleAgentsExtension/Pages/Agents/AgentsListPage.cs"; $text = Get-Content $path -Raw; $getItems = [regex]::Match($text, 'public override IListItem\[\] GetItems\(\)(?s).*?\n    }').Value; [pscustomobject]@{ agentsChanged = $text.Contains('agents.Changed'); providersChanged = $text.Contains('providers.Changed'); cachedAgentsLoad = $text.Contains('_cachedAgents = agents.Load()'); cachedProvidersLoad = $text.Contains('_cachedProviders = providers.Load()'); raiseItemsChanged = ([regex]::Matches($text, 'RaiseItemsChanged\(-1\)').Count); getItemsCallsAgentsLoad = $getItems.Contains('agents.Load()'); stablePinnedId = $text.Contains('Id = $"little-agents.pinned.{GetStableKey(title)}"'); toastWiredInT30 = $text.Contains('CommandResult.ShowToast("Wired in T30")'); realNavigation = [regex]::IsMatch($text, 'AgentEditFormPage|ProvidersListPage|GoToPage|SettingsPage') } | Format-List
Result:
agentsChanged           : True
providersChanged        : True
cachedAgentsLoad        : True
cachedProvidersLoad     : True
raiseItemsChanged       : 3
getItemsCallsAgentsLoad : False
stablePinnedId          : True
toastWiredInT30         : True
realNavigation          : False

Command:
lsp_diagnostics on LittleAgentsExtension/Pages/Agents/AgentsListPage.cs
Result:
MCP error -32000: Connection closed

Manual QA:
- manual_smoke_unavailable: Command Palette desktop UI was not run from this worker session.
- Data-shaped evidence confirms T21 source/build acceptance, but human/host smoke is still useful before checkbox checkoff because runtime UI refresh behavior depends on the Command Palette host.

Adversarial notes:
- stale_state: subscriptions reload both affected caches before RaiseItemsChanged(-1).
- misleading_success_output: build exit code was recorded explicitly as EXITCODE=0.
- dirty_worktree/no git limitation: git status showed the workspace contents as untracked, so git diff could not be used as authoritative evidence.
- stale generated/cached artifact: build regenerated Debug x64 output successfully.
- hung_commands: no command hung; build completed in about 10 seconds.
