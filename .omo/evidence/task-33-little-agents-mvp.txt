Task 33 evidence - ChatRunPage initial-run flow

Changed files:
- LittleAgentsExtension/Pages/Run/ChatRunPage.cs
- LittleAgentsExtension.Tests/ChatRunPageInitialRunTests.cs
- LittleAgentsExtension.Tests/ChatRunPageScaffoldTests.cs
- .omo/evidence/task-33-little-agents-mvp.txt

Implementation summary:
- ChatRunPage constructor now accepts optional Func<Task<string?>> readClipboardAsync seam, defaulting to new ClipboardReader().TryGetTextAsync().
- GetContent() activates the page first, starts one non-blocking InitializeFirstRunAsync dispatch on the first call, sets _output.Body to "_Preparing..._", and immediately returns _output.
- InitializeFirstRunAsync reads clipboard once, shows RunInputForm for real {input} placeholders without starting a stream, or renders the no-input template and marks stream-start.
- RunInputForm renders one Adaptive Card Input.Text with id "Input" and a submit action; SubmitForm parses "Input", calls the callback, and returns CommandResult.KeepOpen().
- T33 StartStream stub only sets _streamTask = Task.CompletedTask; it does not call _llm.StreamAsync, build requests, mutate _history, create CTS, map errors, or append streaming markdown.
- Existing ChatRunPageScaffoldTests helper now passes the clipboard seam so tests do not call the real clipboard.

Automated test/build results:
- Red check before implementation: dotnet test "LittleAgentsExtension.Tests/LittleAgentsExtension.Tests.csproj" -c Debug -p:Platform=x64 2>&1; "EXITCODE=$LASTEXITCODE" -> EXITCODE=1, compile failed because ChatRunPage lacked readClipboardAsync parameter.
- Final tests: dotnet test "LittleAgentsExtension.Tests/LittleAgentsExtension.Tests.csproj" -c Debug -p:Platform=x64 2>&1; "EXITCODE=$LASTEXITCODE" -> EXITCODE=0, 60 passed, 0 failed, 0 skipped.
- Final build: dotnet build "LittleAgentsExtension/LittleAgentsExtension.csproj" -c Debug -p:Platform=x64 2>&1; "EXITCODE=$LASTEXITCODE" -> EXITCODE=0, 0 warnings, 0 errors.

Source/boundary checks:
- Line counts: ChatRunPage.cs lines=190 pure=167; ChatRunPageInitialRunTests.cs lines=195 pure=161; ChatRunPageScaffoldTests.cs lines=168 pure=142.
- where.exe rg -> EXITCODE=1 in this environment, so source checks used PowerShell Select-String from bash.
- Select-String forbidden T34 markers in ChatRunPage.cs for _llm.StreamAsync, BuildRequest, _history.Add, MapError, UpdateOutput, AppendMarkdown, new CancellationTokenSource, Dispose( -> no matches, EXITCODE=0.
- T33 marker check confirmed readClipboardAsync, ClipboardReader default, RequiresInput, RunInputForm, and StartStream are present only in the relevant files.
- No edits made to .omo/plans/little-agents-mvp.md, .omo/start-work/ledger.jsonl, or .omo/boulder.json.

Coverage notes:
- Tests cover immediate first GetContent placeholder with pending clipboard, duplicate GetContent before init completion, {input} form without stream start, input submission rendering with prior selection, no-input auto render/start, escaped {{input}} literal handling, and cancellation of prior active page before input submit.
