Task 34 evidence - ChatRunPage streaming loop, cancellation, history invariant, and D-ERR mapping

Changed files:
- LittleAgentsExtension/Pages/Run/ChatRunPage.cs
- LittleAgentsExtension.Tests/ChatRunPageStreamingTests.cs
- LittleAgentsExtension.Tests/ChatRunPageInitialRunTests.cs
- LittleAgentsExtension.Tests/ChatRunPageScaffoldTests.cs
- .omo/evidence/task-34-little-agents-mvp.txt

Implementation summary:
- Replaced the T33 StartStream stub with real streaming orchestration.
- StartStream defensively activates the page, cancels the previous CTS without disposing it, creates a new stream-owned CTS, appends the user turn, snapshots BuildRequest synchronously, and starts RunStreamAsync.
- BuildRequest prepends System(_settings.SystemPrefix + _agent.SystemPrompt) on every call and copies only page-local user/assistant _history after it.
- RunStreamAsync updates MarkdownContent through UpdateOutput, appends exactly one assistant turn on successful completion, and does not append assistant history on cancellation or errors.
- Superseded streams are guarded by ReferenceEquals(_cts, owner) before output/history commits; each stream task disposes only its own CTS and clears _cts only when still current.
- D-ERR mapping checks TLS first, then generic SDK-style Status/StatusCode properties across the exception chain, then HttpRequestException, then generic exception.
- Error text is API-key scrubbed before truncation; toast text uses the same priority order with short messages.

Automated verification:
- dotnet test "LittleAgentsExtension.Tests\\LittleAgentsExtension.Tests.csproj" -c Debug -p:Platform=x64 -> 66 passed, 0 failed, 0 skipped.
- dotnet build "LittleAgentsExtension\\LittleAgentsExtension.csproj" -c Debug -p:Platform=x64 -> 0 warnings, 0 errors.

Independent source checks:
- ChatRunPage.cs line count: 243, satisfying the <= 250 acceptance ceiling.
- ChatRunPageStreamingTests.cs covers successful 3-chunk stream, cancellation after partial output, network error, OpenAI status error, synthetic StatusCode error, TLS-before-network classification, API-key scrubbing in error blocks, toast priority text, superseded stream stale-output/history guard, and shared RunSessionCoordinator cancellation.
- Existing T33 tests continue to cover input-gated first render cancelling a prior active page before submit.

Notes:
- The implementation intentionally keeps _lastToastText private for test verification until real command/page toast plumbing is expanded in later tasks.
- No manual smoke is required for T34; manual/render gates remain T22, T28, T44, and later integrated QA.
