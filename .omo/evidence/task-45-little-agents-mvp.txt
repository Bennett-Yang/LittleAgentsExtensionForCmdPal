Task 45 implementation evidence

Baseline missing files before implementation:
- LittleAgentsExtension/Storage/SecretStoreSmokeTest.cs: missing
- LittleAgentsExtension/Storage/DpapiSecretStore.cs: missing
- LittleAgentsExtension/Storage/SecretStoreFactory.cs: missing
- LittleAgentsExtension.Tests/DpapiSecretStoreTests.cs: missing

Changed files:
- Directory.Packages.props
- LittleAgentsExtension/LittleAgentsExtension.csproj
- LittleAgentsExtension/Program.cs
- LittleAgentsExtension/Storage/DpapiSecretStore.cs
- LittleAgentsExtension/Storage/SecretStoreFactory.cs
- LittleAgentsExtension/Storage/SecretStoreSmokeTest.cs
- LittleAgentsExtension.Tests/DpapiSecretStoreTests.cs
- .omo/evidence/spike-vault-mta.md
- .omo/evidence/task-45-little-agents-mvp.txt

Build command:
dotnet build LittleAgentsExtension/LittleAgentsExtension.csproj -c Debug -p:Platform=x64 2>&1; "EXITCODE=$LASTEXITCODE"

Build result:
EXITCODE=0
3 warnings, 0 errors. Warnings were existing/non-task analyzer warnings in `ILlmChatClient.cs`, generated CsWinRT code, and `ClipboardReader.cs`.

DPAPI test command:
dotnet test LittleAgentsExtension.Tests/LittleAgentsExtension.Tests.csproj --filter FullyQualifiedName~DpapiSecretStoreTests 2>&1; "EXITCODE=$LASTEXITCODE"

DPAPI test result:
EXITCODE=0
Passed: 3, Failed: 0, Skipped: 0, Total: 3, Duration: 74 ms.

Smoke command:
& "LittleAgentsExtension/bin/x64/Debug/net9.0-windows10.0.26100.0/win-x64/LittleAgentsExtension.exe" --smoke-vault; "EXITCODE=$LASTEXITCODE"

Smoke result:
EXITCODE was blank because the app is built as WinExe, but the command returned and wrote `OK` to `C:\Users\benne\AppData\Local\Temp\LittleAgentsExtension\LittleAgents\spike-vault-mta.log`.

Leak grep command:
Select-String -Path LittleAgentsExtension/Storage/SecretStoreSmokeTest.cs,LittleAgentsExtension/Storage/DpapiSecretStore.cs,LittleAgentsExtension/Storage/SecretStoreFactory.cs -Pattern 'p|sk-|apiKey'

Leak grep result:
No `sk-` matches. `apiKey` appears only as the ISecretStore parameter name in DpapiSecretStore.Set. `p` matches are broad regex hits in namespace/type/member words plus the required smoke password literal `"p"`; the smoke result/log writes only `OK` or sanitized exception type/HResult, not the plaintext.

Manual QA:
- Data-shaped QA artifact: .omo/evidence/spike-vault-mta.md
- Data-shaped QA artifact: .omo/evidence/task-45-little-agents-mvp.txt

Cleanup:
- No `.gitkeep` files removed; the existing `.gitkeep` files are under Pages folders and are unrelated to this storage/smoke task.
- Test secrets use unique provider ids and delete their files in finally blocks.

Notes:
- First build attempt failed because `System.Security.Cryptography.ProtectedData` was not referenced. Added the central package version and project package reference, then build succeeded.
- A repeated build run overlapped with another dotnet process and hit a transient ReadyToRun file lock; rerunning the requested build command after the test process completed succeeded.
- Changed code files are below the 250 pure LOC ceiling: DpapiSecretStoreTests 65, DpapiSecretStore 49, SecretStoreFactory 48, SecretStoreSmokeTest 55, Program 35.

Verifier blocker fix evidence

Fixes:
- Replaced `[Fact]` plus early `OperatingSystem.IsWindows()` returns with `WindowsOnlyFactAttribute`, which sets xUnit `Skip = "Requires Windows DPAPI."` on non-Windows instead of reporting misleading passes.
- Changed DPAPI secret filenames from raw `providerId` to `Convert.ToHexString(SHA256.HashData(Encoding.UTF8.GetBytes(providerId))) + ".bin"`; DPAPI entropy still uses the original providerId UTF-8 bytes.
- Added malformed providerId traversal coverage asserting `GetSecretPath` stays under `PathHelper.LocalStateDir\secrets` and does not include raw traversal text in the filename.

Verifier build command:
dotnet build LittleAgentsExtension/LittleAgentsExtension.csproj -c Debug -p:Platform=x64 2>&1; "EXITCODE=$LASTEXITCODE"

Verifier build result:
EXITCODE=0
3 warnings, 0 errors. Warnings were existing/non-task analyzer warnings in `ILlmChatClient.cs`, generated CsWinRT code, and `ClipboardReader.cs`.

Verifier DPAPI test command:
dotnet test LittleAgentsExtension.Tests/LittleAgentsExtension.Tests.csproj --filter FullyQualifiedName~DpapiSecretStoreTests 2>&1; "EXITCODE=$LASTEXITCODE"

Verifier DPAPI test result:
EXITCODE=0
Passed: 4, Failed: 0, Skipped: 0, Total: 4, Duration: 68 ms.

Verifier smoke command:
& "LittleAgentsExtension\bin\x64\Debug\net9.0-windows10.0.26100.0\win-x64\LittleAgentsExtension.exe" --smoke-vault; "EXITCODE=$LASTEXITCODE"

Verifier smoke result:
EXITCODE was blank because the app is built as WinExe, but the command returned and wrote `OK` to `C:\Users\benne\AppData\Local\Temp\LittleAgentsExtension\LittleAgents\spike-vault-mta.log`.

Windows-only/safe path grep command:
Select-String -Path LittleAgentsExtension.Tests/DpapiSecretStoreTests.cs,LittleAgentsExtension/Storage/DpapiSecretStore.cs -Pattern 'WindowsOnlyFact|Skip|SHA256|Convert.ToHexString|GetSecretPath|Path.Combine'

Windows-only/safe path grep result:
Matches showed `[WindowsOnlyFact]` on the three DPAPI-exercising tests, `Skip = "Requires Windows DPAPI."`, SHA256/Convert.ToHexString filename encoding, and Path.Combine only under `PathHelper.LocalStateDir\secrets`.

Verifier leak grep command:
Select-String -Path LittleAgentsExtension/Storage/DpapiSecretStore.cs,LittleAgentsExtension.Tests/DpapiSecretStoreTests.cs -Pattern 'sk-|apiKey|"p"'

Verifier leak grep result:
No `sk-` or `"p"` matches. `apiKey` appears only as the ISecretStore parameter name in DpapiSecretStore.Set and is encoded/protected, not logged.

Verifier cleanup:
- Changed code files remain below the 250 pure LOC ceiling: DpapiSecretStore 51, DpapiSecretStoreTests 74.
